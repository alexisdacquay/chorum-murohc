"""The browser-journey harness: a real browser driving the real product.

Nothing in this package is imported by product code. It exists so that the
household journeys that matter can be driven end to end in headless Chrome,
against a real Django server and the real built frontend on one origin.

`README.md` in this directory is the entry point for a reader.
"""
